/**
 * Supabase infrastructure — client, types, and auth utilities
 */

export { supabase, isSupabaseConfigured } from './client'
export type { Database, Profile, TrialCode, Tier, TrialStatus, CodeStatus } from './types'
